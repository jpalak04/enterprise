import { createRouteRef } from '@backstage/core-plugin-api';

export const cardRouteRef = createRouteRef({
  id: 'ce-deployment-cards',
});
export const rootRouteRef = createRouteRef({
  id: 'ce-deployment-installer',
});
